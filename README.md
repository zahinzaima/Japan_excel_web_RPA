# 🚀 Setup and Run Project for the 1st time

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
pytest -v -s
```
# Next time run

```bash
.venv\Scripts\activate
pytest -v -s
```
